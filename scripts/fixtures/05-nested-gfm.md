# Nested GFM

Task list inside a blockquote inside a list:

- Top-level item one
  - Nested item with a **bold** word
    > Quoted line with an inline `code` span and a [link](https://example.com).
    >
    > - [x] Inner done
    > - [ ] Inner todo
- Top-level item two
  1. Ordered nested
  2. Ordered nested with $\sqrt{2}$ math
     - Even deeper bullet with ~~struck-through~~ text

Mixed inline:

A paragraph with **bold _and italic_**, `inline code`, $\pi$, and a [link with `code`](https://example.com).
